<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><b>TURBOLY-APP <%= Time.now.in_time_zone('Jakarta').to_date %></b></a>
    <%
=begin%>
 <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button> 
<%
=end%>
    <div class="collapse_ navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if current_user %>
            <li class="nav-item">
                <%= link_to "Home", root_path, class: "nav-link #{request[:controller] == 'homes' ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
                <%= link_to "Task", tasks_path, class: "nav-link #{request[:controller] == 'tasks' ? 'active' : ''}" %>
            </li>
            <li class="nav-item">
                <%= link_to(todays_path, class: "nav-link #{request[:controller] == 'todays' ? 'active' : ''}") do %>
                  Today
                  <span class="badge badge-light <%= todays.length == 0 ? 'bg-secondary' : 'bg-danger' %>"><%= todays.length %></span>
                <% end %>
            </li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if current_user %>
                <li class="nav-item nav-link text-light">
                  (<%= current_user.name %>)
                </li>
                <li class="nav-item">
                    <%= link_to "Logout", logout_path, class: "nav-link #{request[:controller] == 'sessions' ? 'active' : ''}", data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
                </li>
            <% else %>
                <li class="nav-item">
                    <%= link_to "Login", login_path, class: "nav-link #{request[:controller] == 'sessions' ? 'active' : ''}" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Signup", '/users/new', class: "nav-link #{request[:controller] == 'users' ? 'active' : ''}" %>
                </li>
            <% end %>
        </ul>
      </form>
    </div>
  </div>
</nav>